<%= form_tag(quizzes_path, method: "post") do %>
  <div class="text-center">
    <%= submit_tag("Start Quiz", class: 'btn btn-info', remote: true, 'data-toggle': 'modal', 'data-target': '#exampleModalCenter') %>
  </div>
<% end %>
